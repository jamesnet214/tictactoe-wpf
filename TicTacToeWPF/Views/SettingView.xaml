<UserControl x:Class="TicTacToeWPF.Views.SettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Background="White" d:DesignWidth="400" d:DesignHeight="300">
    <Grid Margin="0 70 0 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="30 0 0 0">
            <TextBlock Style="{StaticResource TXB.PLAYER.01}"/>
            <TextBox Style="{StaticResource TXT.PLAYER}"/>
        </StackPanel>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="30 50 0 0">
            <TextBlock Style="{StaticResource TXB.PLAYER.02}"/>
            <TextBox Style="{StaticResource TXT.PLAYER}"/>
        </StackPanel>
        <Button Style="{StaticResource BTN.START}"/>
        <Button Style="{StaticResource BTN.RESTART}"/>
    </Grid>
</UserControl>
